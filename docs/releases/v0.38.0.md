# TypeScript WSDL Client v0.38.0

## Dependency Floor Refresh

This release refreshes maintained dependency floors after the merged dependency updates and keeps generated app scaffolds aligned with the package baseline.

## What This Improves

Developers get current dependency minimums in both the published package and newly scaffolded Fastify gateway apps. The runtime YAML dependency moves to the latest `js-yaml` patch in the current line, while the development and scaffold stack pick up the maintained Fastify, Node type, and `tsx` releases.

The 1.0 roadmap remains focused on release-candidate gates. This release does not change generated SOAP, OpenAPI, or gateway behavior.

## Highlights

- Updates `js-yaml` to `5.2.1`.
- Updates `fastify` to `5.9.0`.
- Updates `@types/node` to `26.1.0`.
- Updates `tsx` to `4.23.0`.
- Updates generated app scaffold pins for matching dependency floors.
- Aligns roadmap documentation with the `0.38.0` release state.

## Upgrade Notes

No special upgrade steps.

## Validation

- CI passed.
- NPM package contents were validated.
- Agent skill artifact was validated and packaged.
- Release preflight passed against the target tag.

## Notes

Release tag: `v0.38.0`.
